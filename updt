# rút gọn
main=https://raw.githubusercontent.com/vntuhu/ytbdl/main
home=/data/data/com.termux/files/home
bin=/data/data/com.termux/files/usr/bin

# update
rm -r $bin/updt
wget $main/updt -P $bin/
chmod +x $bin/updt

# termux-url-opener
rm -r $home/bin/termux-url-opener
wget $main/termux-url-opener -P $home/bin
chmod +x $home/bin/termux-url-opener

# ytbdl config
rm -r $home/.config/youtube-dl/config
wget $main/config -P $home/.config/youtube-dl

# ytbdl short
rm -r $bin/ytb
wget $main/ytb -P $bin/
chmod +x $bin/ytb

# video converter
rm -r $bin/conv
wget $main/conv -P $bin
chmod +x $bin/conv

# resize video
rm -r $bin/resize
wget $main/resize -P $bin/
chmod +x $bin/resize

# ytbdl option
rm -r $bin/yto
wget $main/yto -P $bin
chmod +x $bin/yto

echo "Done..."
